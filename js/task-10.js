function getRandomHexColor() {
  return `#${Math.floor(Math.random() * 16777215).toString(16)}`;
}
const numberRef = document.querySelector("input");
const btnsRef = document.querySelectorAll("button");
const btnCreateRef = btnsRef[0];
const btnClearRef = btnsRef[1];
numberRef.addEventListener("change", (event) => {

});
console.log(number);